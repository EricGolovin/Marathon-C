#include <stdio.h>
#include "mx_strlen.c"

char *mx_strcat(char *s1, const char *s2) {
	int mx_strlen(const char *s);
	while (*s1 != '\0') {
		s1++;
	}

	while ((*s1++ = *s2++) != '\0');

	return s1;
}
